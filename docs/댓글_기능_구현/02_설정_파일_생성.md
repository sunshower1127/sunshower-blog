# Phase 2: 설정 파일 생성

giscus의 설정값을 관리하는 TypeScript 설정 파일을 생성합니다.

## 기존 패턴 참고

프로젝트에는 이미 Disqus 설정 파일이 있습니다:

**파일**: [src/data/disqus.config.ts](../../src/data/disqus.config.ts)

```typescript
interface DisqusConfig {
  enabled: boolean
  shortname: string
}

export const disqusConfig: DisqusConfig = {
  enabled: false,
  shortname: ''
}
```

동일한 패턴으로 giscus 설정을 만들겠습니다.

---

## 생성할 파일

**파일 경로**: `src/data/giscus.config.ts`

**내용**:

```typescript
interface GiscusConfig {
  enabled: boolean
  repo: string          // GitHub 저장소 (형식: "username/repo")
  repoId: string        // giscus.app에서 복사한 data-repo-id
  category: string      // Discussion 카테고리 이름
  categoryId: string    // giscus.app에서 복사한 data-category-id
  mapping: string       // 매핑 방식 (pathname 권장)
  reactionsEnabled: boolean  // 리액션 활성화 여부
  theme: string         // 테마 (preferred_color_scheme: 자동 다크모드)
  lang: string          // 언어 (ko: 한국어)
}

export const giscusConfig: GiscusConfig = {
  enabled: true,
  repo: '',            // ⚠️ 여기에 입력: "username/sunshower-blog"
  repoId: '',          // ⚠️ 여기에 입력: giscus.app에서 복사한 R_xxx
  category: '',        // ⚠️ 여기에 입력: "General" 또는 "Comments"
  categoryId: '',      // ⚠️ 여기에 입력: giscus.app에서 복사한 DIC_xxx
  mapping: 'pathname',
  reactionsEnabled: true,
  theme: 'preferred_color_scheme',
  lang: 'ko'
}
```

---

## 입력해야 할 값

이전 단계([01_사전_준비.md](./01_사전_준비.md))에서 복사한 값을 다음 위치에 입력하세요:

| 필드 | 입력할 값 | 예시 |
|------|-----------|------|
| `repo` | giscus.app의 `data-repo` 값 | `"sunwoo-kim/sunshower-blog"` |
| `repoId` | giscus.app의 `data-repo-id` 값 | `"R_kgDOxxxxxx"` |
| `category` | giscus.app의 `data-category` 값 | `"General"` 또는 `"Comments"` |
| `categoryId` | giscus.app의 `data-category-id` 값 | `"DIC_kwDOxxxxxx"` |

⚠️ **주의**:
- 값은 **따옴표로 감싸야** 합니다 (`"값"`)
- 빈 문자열(`''`)로 두면 giscus가 작동하지 않습니다

---

## 설정 필드 설명

### `enabled: boolean`
- giscus 기능 활성화 여부
- `true`: 댓글 위젯 표시
- `false`: 댓글 위젯 숨김

### `repo: string`
- GitHub 저장소 전체 경로
- 형식: `"소유자/저장소명"`
- 예: `"sunwoo-kim/sunshower-blog"`

### `repoId: string`
- GitHub GraphQL API에서 사용하는 저장소 고유 ID
- giscus.app에서 자동 생성
- 형식: `"R_kgDO"` 로 시작

### `category: string`
- GitHub Discussions의 카테고리 이름
- 댓글이 저장될 Discussion 카테고리
- 권장: `"General"` 또는 `"Comments"`

### `categoryId: string`
- 카테고리 고유 ID
- giscus.app에서 자동 생성
- 형식: `"DIC_kwDO"` 로 시작

### `mapping: string`
- 블로그 포스트와 Discussion을 연결하는 방식
- `"pathname"`: URL 경로 기반 (권장)
  - 예: `/post/astro` → 고유 Discussion 생성
- 다른 옵션: `"url"`, `"title"`, `"og:title"`, `"specific"`, `"number"`

### `reactionsEnabled: boolean`
- 댓글에 이모지 리액션(👍❤️🎉) 활성화 여부
- `true`: 리액션 버튼 표시

### `theme: string`
- giscus 위젯 테마
- `"preferred_color_scheme"`: 브라우저 설정 따름 (자동 다크모드)
- 다른 옵션: `"light"`, `"dark"`, `"dark_dimmed"` 등

### `lang: string`
- giscus UI 언어
- `"ko"`: 한국어
- 다른 옵션: `"en"`, `"ja"`, `"zh-CN"` 등

---

## TypeScript 타입 체크

설정 파일을 작성한 후, TypeScript 컴파일러가 자동으로 타입을 검증합니다:

```bash
# 타입 체크 (선택 사항)
pnpm run build
```

오류가 있다면 `tsconfig.json`에서 정의된 타입 규칙을 위반한 것입니다.

---

## 예시 (완성된 설정)

```typescript
export const giscusConfig: GiscusConfig = {
  enabled: true,
  repo: 'sunwoo-kim/sunshower-blog',
  repoId: 'R_kgDOH1234567',
  category: 'General',
  categoryId: 'DIC_kwDOH1234567',
  mapping: 'pathname',
  reactionsEnabled: true,
  theme: 'preferred_color_scheme',
  lang: 'ko'
}
```

---

## 체크리스트

- [ ] `src/data/giscus.config.ts` 파일 생성
- [ ] 4개 필수 값 입력 완료:
  - [ ] `repo`
  - [ ] `repoId`
  - [ ] `category`
  - [ ] `categoryId`
- [ ] 모든 값이 따옴표로 감싸짐
- [ ] `enabled: true`로 설정

---

## 다음 단계
📄 [03_컴포넌트_생성.md](./03_컴포넌트_생성.md) - giscus Astro 컴포넌트 생성
